export const mod_origin_mark = {
  methods: {
    ev_origin_mark_click(params, event) {
      // console.log(params)
      // alert(1)
      // const origin_mark_attrs = this.origin_mark_attrs_from(params.general_mark_pos_key)
      // this.$refs.sp_object.mut_origin_mark_list.toggle(origin_mark_attrs)
    },
  },
  computed: {
  },
}
